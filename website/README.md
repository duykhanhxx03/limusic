# Limusic website

Static landing page (Vite + React + Tailwind + React Bits), deployed to GitHub Pages at https://duykhanhxx03.github.io/limusic/ by .github/workflows/website.yml on every push to master touching website/.

```bash
pnpm install
pnpm dev      # local dev
pnpm build    # outputs dist/
```
